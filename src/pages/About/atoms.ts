import styled from "styled-components"

export const AboutContainer = styled.main`
  display: flex;
  align-items: center;
  gap: 480px;
  padding: 0 210px;

  @media (max-width: 520px) {
    padding: 0 35px;
  }
`
